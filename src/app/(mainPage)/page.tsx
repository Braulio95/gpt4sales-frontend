import CardContainer from "../components/BookContainer/BookContainer";

export default function Home() {
    return (
        <main className="flex min-h-screen flex-col items-center justify-between">
            <CardContainer />
        </main>
    );
}
